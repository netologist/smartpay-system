# Auto-generated by scripts/ci/sync-flyway-configmaps.sh — DO NOT EDIT.
# Source: smartpay-payment-service/src/main/resources/db/migration
apiVersion: v1
kind: ConfigMap
metadata:
  name: smartpay-payment-flyway-scripts
  namespace: smartpay
  labels:
    app.kubernetes.io/part-of: smartpay-platform
    app.kubernetes.io/component: db-migration
    app.kubernetes.io/instance: payment
data:
  V1__init_transactional_outbox.sql: |-
    -- ==============================================================================
    -- Flyway Migration: V1__init_transactional_outbox.sql
    -- Domain: Transactional Outbox Pattern for Guaranteed At-Least-Once Kafka Delivery
    -- Database: PostgreSQL 16
    -- ==============================================================================
    
    CREATE TABLE transactional_outbox (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        aggregate_type VARCHAR(64) NOT NULL,
        aggregate_id VARCHAR(64) NOT NULL,
        event_type VARCHAR(64) NOT NULL,
        payload JSONB NOT NULL,
        created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
        processed_at TIMESTAMP WITH TIME ZONE NULL
    );
    
    -- Partial index strictly on unprocessed events: guarantees O(1) polling speed
    -- when executing: SELECT * FROM transactional_outbox WHERE processed_at IS NULL ORDER BY created_at FOR UPDATE SKIP LOCKED
    CREATE INDEX idx_outbox_unprocessed ON transactional_outbox(created_at ASC)
    WHERE processed_at IS NULL;
    
    CREATE INDEX idx_outbox_aggregate ON transactional_outbox(aggregate_type, aggregate_id);
  V2__init_idempotency_records.sql: |-
    -- ==============================================================================
    -- Flyway Migration: V2__init_idempotency_records.sql
    -- Domain: Distributed Two-Tier Idempotency & Tamper Detection
    -- Database: PostgreSQL 16
    -- ==============================================================================
    
    CREATE TABLE idempotency_records (
        tenant_id VARCHAR(64) NOT NULL,
        idempotency_key VARCHAR(128) NOT NULL,
        request_hash VARCHAR(64) NOT NULL, -- SHA-256 fingerprint of request body
        status VARCHAR(16) NOT NULL,
        response_code INT NULL,
        response_body JSONB NULL,
        created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
        expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
        PRIMARY KEY (tenant_id, idempotency_key),
        CONSTRAINT chk_idemp_status CHECK (status IN ('PROCESSING', 'COMPLETED', 'FAILED'))
    );
    
    -- Index for automated background TTL purging
    CREATE INDEX idx_idempotency_expires ON idempotency_records(expires_at);
  V3__init_payments.sql: |-
    -- ==============================================================================
    -- Flyway Migration: V3__init_payments.sql
    -- Domain: Payment records — source of truth for payment lifecycle state
    -- Database: PostgreSQL 16
    -- ==============================================================================
    
    CREATE TABLE payments (
        id               UUID         NOT NULL PRIMARY KEY,
        tenant_id        VARCHAR(64)  NOT NULL,
        account_id       VARCHAR(64)  NOT NULL,
        amount_minor     BIGINT       NOT NULL,               -- minor currency units (e.g. cents)
        currency         VARCHAR(3)   NOT NULL,               -- ISO 4217
        status           VARCHAR(16)  NOT NULL,
        idempotency_key  VARCHAR(128) NOT NULL,
        end_to_end_id    VARCHAR(128),
        created_at       TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
        updated_at       TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
        CONSTRAINT chk_payment_status CHECK (status IN ('INITIATED', 'PROCESSING', 'SETTLED', 'FAILED')),
        CONSTRAINT uq_payment_idempotency UNIQUE (tenant_id, idempotency_key)
    );
    
    CREATE INDEX idx_payments_tenant_status ON payments (tenant_id, status);
    CREATE INDEX idx_payments_account ON payments (account_id);
