apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartpay-payment-service
  namespace: smartpay
  labels:
    app.kubernetes.io/name: smartpay-payment-service
    app.kubernetes.io/component: payment-engine
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: smartpay-payment-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: smartpay-payment-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: payment
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          image: ghcr.io/hozgan/smartpay/smartpay-payment-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8082
          envFrom:
            - configMapRef:
                name: smartpay-common-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres.smartpay.svc.cluster.local:5432/smartpay_db?currentSchema=payment"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: smartpay-db-app-credentials
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smartpay-db-app-credentials
                  key: password
          resources:
            requests:
              cpu: 150m
              memory: 384Mi
            limits:
              cpu: 1000m
              memory: 768Mi
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 36
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: smartpay-payment-service
  namespace: smartpay
  labels:
    app.kubernetes.io/name: smartpay-payment-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: smartpay-payment-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
