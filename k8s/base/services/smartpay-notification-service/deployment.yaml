apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartpay-notification-service
  namespace: smartpay
  labels:
    app.kubernetes.io/name: smartpay-notification-service
    app.kubernetes.io/component: notification-engine
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: smartpay-notification-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: smartpay-notification-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: notification
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          image: ghcr.io/hozgan/smartpay/smartpay-notification-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8087
          envFrom:
            - configMapRef:
                name: smartpay-common-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres.smartpay.svc.cluster.local:5432/smartpay_db"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: smartpay-db-app-credentials
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smartpay-db-app-credentials
                  key: password
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8087
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8087
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8087
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: smartpay-notification-service
  namespace: smartpay
  labels:
    app.kubernetes.io/name: smartpay-notification-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: smartpay-notification-service
  ports:
    - name: http
      port: 8087
      targetPort: 8087
