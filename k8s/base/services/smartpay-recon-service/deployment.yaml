apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartpay-recon-service
  namespace: smartpay
  labels:
    app.kubernetes.io/name: smartpay-recon-service
    app.kubernetes.io/component: recon-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: smartpay-recon-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: smartpay-recon-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: recon
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          image: ghcr.io/hozgan/smartpay/smartpay-recon-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8085
          envFrom:
            - configMapRef:
                name: smartpay-common-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres.smartpay.svc.cluster.local:5432/smartpay_db"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: smartpay-db-app-credentials
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smartpay-db-app-credentials
                  key: password
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 36
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: smartpay-recon-service
  namespace: smartpay
  labels:
    app.kubernetes.io/name: smartpay-recon-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: smartpay-recon-service
  ports:
    - name: http
      port: 8085
      targetPort: 8085
