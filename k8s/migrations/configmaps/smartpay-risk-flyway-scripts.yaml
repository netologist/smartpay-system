# Auto-generated by scripts/ci/sync-flyway-configmaps.sh — DO NOT EDIT.
# Source: smartpay-risk-service/src/main/resources/db/migration
apiVersion: v1
kind: ConfigMap
metadata:
  name: smartpay-risk-flyway-scripts
  namespace: smartpay
  labels:
    app.kubernetes.io/part-of: smartpay-platform
    app.kubernetes.io/component: db-migration
    app.kubernetes.io/instance: risk
data:
  V1__init_carrier_risk_and_fraud.sql: |-
    -- ==============================================================================
    -- Flyway Migration: V1__init_carrier_risk_and_fraud.sql
    -- Domain: Risk & Underwriting Bounded Context
    -- Database: PostgreSQL 16
    -- ==============================================================================
    
    CREATE EXTENSION IF NOT EXISTS "pgcrypto";
    
    -- 1. Carrier Risk Profiles
    CREATE TABLE carrier_risk_profiles (
        carrier_id UUID PRIMARY KEY,
        status VARCHAR(24) NOT NULL DEFAULT 'ACTIVE',
        max_credit_limit_pence BIGINT NOT NULL DEFAULT 0,
        current_active_factoring_pence BIGINT NOT NULL DEFAULT 0,
        currency VARCHAR(3) NOT NULL DEFAULT 'GBP',
        base_risk_score INT NOT NULL DEFAULT 18,
        historical_default_count INT NOT NULL DEFAULT 0,
        bank_account_number VARCHAR(32),
        bank_sort_code VARCHAR(16),
        last_known_ip VARCHAR(64),
        created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
        updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
        CONSTRAINT chk_carrier_status CHECK (status IN ('ACTIVE', 'SUSPENDED', 'BLACKLISTED', 'SANCTIONED')),
        CONSTRAINT chk_credit_limit_non_neg CHECK (max_credit_limit_pence >= 0),
        CONSTRAINT chk_active_factoring_non_neg CHECK (current_active_factoring_pence >= 0)
    );
    
    CREATE INDEX idx_carrier_risk_profiles_status ON carrier_risk_profiles(status);
    
    -- 2. Shipper Risk Profiles (Supporting Collusion & Sanction Analysis)
    CREATE TABLE shipper_risk_profiles (
        shipper_id UUID PRIMARY KEY,
        status VARCHAR(24) NOT NULL DEFAULT 'ACTIVE',
        bank_account_number VARCHAR(32),
        bank_sort_code VARCHAR(16),
        last_known_ip VARCHAR(64),
        created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
        updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
        CONSTRAINT chk_shipper_status CHECK (status IN ('ACTIVE', 'SUSPENDED', 'BLACKLISTED'))
    );
    
    CREATE INDEX idx_shipper_risk_profiles_status ON shipper_risk_profiles(status);
    
    -- 3. Fraud Rule Evaluations Audit Table
    CREATE TABLE fraud_rule_evaluations (
        id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
        carrier_id UUID NOT NULL,
        shipper_id UUID NOT NULL,
        invoice_amount_pence BIGINT NOT NULL,
        currency VARCHAR(3) NOT NULL DEFAULT 'GBP',
        risk_score INT NOT NULL,
        risk_tier VARCHAR(16) NOT NULL,
        approved BOOLEAN NOT NULL,
        reasoning VARCHAR(512) NOT NULL,
        rules_triggered JSONB,
        evaluated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
        CONSTRAINT chk_risk_score_bounds CHECK (risk_score >= 0 AND risk_score <= 100),
        CONSTRAINT chk_risk_tier CHECK (risk_tier IN ('LOW', 'MEDIUM', 'HIGH', 'CRITICAL'))
    );
    
    CREATE INDEX idx_fraud_evaluations_carrier_time ON fraud_rule_evaluations(carrier_id, evaluated_at DESC);
    CREATE INDEX idx_fraud_evaluations_tier ON fraud_rule_evaluations(risk_tier);
